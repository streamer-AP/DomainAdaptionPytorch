import numpy as np
from matplotlib import pyplot as plt
import random
def generate_data(l,N,):
    theta=np.random.random(N)*np.pi
    p1=l+np.random.random(N)
    p2=l-np.random.random(N)
    for i in range(N):


if __name__ == "__main__":
    
    plt.